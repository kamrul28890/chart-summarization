# Dataset ZIP Shards

The local working copy contains `part_01.zip` through `part_10.zip`, but those ZIP files are intentionally not committed to GitHub because each file is about 478-542 MB and normal GitHub pushes reject files over 100 MB.

For cluster runs, upload the local ZIP files from this folder alongside the notebook.
